# Bio-Resource-Query
Script made to take a feature class input, select features from a handful of public biological resource datasets (CNDDB, USFWS) within a given distance of the input feature, and export the selection into a feature class and KMZ output.
